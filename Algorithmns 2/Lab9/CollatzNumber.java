class CollatzNumber { // Class to store the number, and it keep track of position
    int number;
    int length;

    CollatzNumber(int number, int length) {
        this.number = number;
        this.length = length;
    }
}